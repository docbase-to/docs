---
title: "Authentication"
---

The Docbase API uses **JWT (JSON Web Tokens)** for authentication. All API requests must be made over **HTTPS** to ensure your data and tokens are encrypted during transit. Requests made over plain HTTP will fail.